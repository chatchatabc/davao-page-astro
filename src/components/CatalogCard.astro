---
interface catalogDataInterface {
  name: string;
  slug?: string;
}
export interface Props {
  catalog?: catalogDataInterface;
}
const { catalog = { name: "No Catalog Name", slug: "" } } = Astro.props;
const { name, slug } = catalog;
---

<li
  class="col-span-1 flex flex-col divide-y divide-gray-200 rounded-lg bg-white text-center shadow overflow-hidden"
>
  <div class="flex flex-1 flex-col">
    <img
      src="/images/chinese restaurant.jpg"
      alt="place"
      class="h-60 object-cover"
    />
    <!-- <h3 class="mt-6 text-sm font-medium text-gray-900">Jane Cooper</h3>
    <dl class="mt-1 flex flex-grow flex-col justify-between">
      <dt class="sr-only">Title</dt>
      <dd class="text-sm text-gray-500">Paradigm Representative</dd>
      <dt class="sr-only">Role</dt>
      <dd class="mt-3">
        <span
          class="rounded-full bg-green-100 px-2 py-1 text-xs font-medium text-green-800"
          >Admin</span
        >
      </dd>
    </dl> -->
  </div>
  <div>
    <div class="-mt-px flex divide-x divide-gray-200">
      <div class="flex w-0 flex-1">
        <a
          href="mailto:janecooper@example.com"
          class="relative -mr-px inline-flex w-0 flex-1 items-center justify-center rounded-bl-lg border border-transparent py-4 text-sm font-medium text-gray-700 hover:text-gray-500"
        >
          <span class="ml-3 text-xl">{name}</span>
        </a>
      </div>
    </div>
  </div>
</li>

<!-- {
  slug ? (
    <a href={`/${slug}`}>
      <div
        class={`text-center  bg-yellow-100 shadow-xl duration-300 ${
          slug && "hover:scale-110 hover:cursor-pointer"
        } py-4 min-h-[200px] flex items-center justify-center`}
      >
        <h3 class="text-xl">{name}</h3>
      </div>
    </a>
  ) : (
    <div
      class="text-center  bg-yellow-100 shadow-xl duration-300 
      py-4 min-h-[200px] flex items-center justify-center"
    >
      <h3 class="text-xl">{name}</h3>
    </div>
  )
} -->
